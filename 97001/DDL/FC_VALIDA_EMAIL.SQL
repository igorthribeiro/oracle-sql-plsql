create or replace FUNCTION FC_VALIDA_EMAIL(P_EMAIL VARCHAR2) RETURN BOOLEAN
IS
BEGIN
   RETURN P_EMAIL IS NOT NULL AND LENGTH(P_EMAIL) > 0
      AND INSTR(P_EMAIL, '@') != 0
      AND LOWER(P_EMAIL) = P_EMAIL;
END;
